# Readme
# Environment
# Requirements
	pip (to install pandas) 
		pandas as pd
		xlrd
	

# Dependancies

a =pd.read_excel('samplebook.xlsx', sheet_name=1)
df=pd.DataFrame(a)
df.c1
df.drop(columns=['
df=df.dropna(axis=1,thresh=10) #drop columns containing'thresh' (10) nulls
